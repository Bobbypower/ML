beta_params=[(0.25,0.25),(1,1),(10,10)]

a=zip(beta_params, ('b', 'r', 'g'))
print(zip(beta_params, ('b', 'r', 'g')))

for q,c in a:
    print(q)
    print(q[1])
    print(c) 
 
#for (a_prior, b_prior), c in zip(beta_params, ('b', 'r', 'g'):
    #print(c)